[tool.ruff]
line-length = 120
select = ["E", "F", "I", "B"]
ignore = ["B008"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
	"app.db.models",
	"app.api.routes.ingest",
	"app.api.routes.agent",
	"app.api.routes.predict",
	"app.api.routes.train",
	"app.api.routes.features",
	"app.api.routes.health",
	"app.services.feature_extraction",
	"app.services.feature_tasks",
	"app.services.training_tasks",
	"app.services.batch_inference_tasks",
	"app.training.train",
	"app.training.synth_data",
	"app.ml.image_features",
	"app.ml.feature_version",
	"app.tests.test_agent_api_integration",
	"app.tests.test_api_endpoints_integration",
	"app.tests.test_ingestion_checkpointing",
]
ignore_errors = true

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["app/tests"]
markers = [
	"unit: fast isolated tests",
	"integration: tests that exercise DB/filesystem/service boundaries",
	"api: HTTP API integration tests",
	"determinism: reproducibility and bit-exactness checks",
]
